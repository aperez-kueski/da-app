name: da_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  da_lib:
    git: #Over Git structure can target to versions
      url: git@github.com:aperez-kueski/da-package.git
      ref: 0.0.1-alpha
  #da_lib: ^0.0.1-alpha #Over pub workspace can target directly
  #da_lib "0.0.1-alpha < 0.0.1" #semantics 11th rule

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

